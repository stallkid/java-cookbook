import java.util.logging.Logger;

public class Condicionais {
    Logger logger = Logger.getLogger("Teste");

    // comparar dois numeros e retornar qual numero é maior
    public static boolean compararDoisNumeros(int x, int y) {
        //logger.info();
        return false;
    }

    // Verificar se o número é par
    public static boolean verificarPar(int x) {
        //logger.info();
        return false;
    }

    // Verificar se o número é impar
    public static boolean verificarImpar(int x) {
        //logger.info();
        return false;
    }

    // Verificar se o primeiro número é par e maior que o segundo
    public static boolean verificarParEMaior(int x, int y) {
        //logger.info();
        return false;
    }

    // Verificar se o primeiro número é par e menor que o segundo
    public static boolean verificarParEMenor(int x, int y) {
        //logger.info();
        return false;
    }

    // Verificar se o número é par ou divisível por 5
    public static boolean verificarSeNumeroParOuDividePorCinco(int x, int y) {
        //logger.info();
        return false;
    }

    // Verificar se o número é primo
    public static boolean verificarSeNumeroPrimo(int x) {
        return false;
    }
    
    // Método main que executa as ações da classe
    public static void main(String[] args) {
        compararDoisNumeros(0, 0);
        verificarPar(0);
        verificarImpar(0);
        verificarParEMaior(0, 0);
        verificarParEMenor(0, 0);
        verificarSeNumeroParOuDividePorCinco(0, 0);
        verificarSeNumeroPrimo(0);
    }
    
}
